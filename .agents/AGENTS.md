# Agent Guidelines

- Always keep responses short, concise, and straight to the point.
- Avoid unnecessary fluff or long paragraphs.
- Never run `git push` automatically. Only execute `git push` when explicitly instructed by the user.

